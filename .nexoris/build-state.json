{
  "product": "Nexoris Technologies Digital Platform",
  "lastUpdated": "2026-06-19T00:00:00Z",
  "currentStage": "stage-6",
  "currentTask": "Stage 6 website-bot path COMPLETE and verified live end to end: NestJS runtime serves /health and the grounded chat SSE endpoint, walking quota -> exact cache -> semantic cache -> hybrid retrieval -> website-bot generation chain (Gemini Flash primary), caching results, with extractive fallback + handoff and plain-notice error handling. PRD 10.7 pricing and off-topic acceptance behaviours confirmed; exact-cache hit confirmed. Remaining in Stage 6: Oge conversational lead capture and the Solution Finder rationale under strict JSON (these pair with Stage 5 and the service-recommender/CRM-worker chains). Not yet exercised live: the Gemini embedding fallback, the generation backups (Groq/Mistral), and the extractive fallback path (primary succeeded throughout). Next suggested: Stage 5 (Contact lead capture + Solution Finder) now that the gateway and DB are proven, or continue Oge lead capture.",
  "lastCommit": "ac2967d",
  "nextAction": "Add git remote origin https://github.com/Nexoris25/nexoristech.git and push main (outward-facing: confirm with the product owner first). Then Stage 6: scaffold apps/oge (NestJS) with config/models.ts pinning the new per-task provider chains (Website Bot, CRM Worker, CMS AI, Service Recommender, Embeddings), the nexoris_oge pgvector store, embedding + ingestion of the existing hardcodedKbSources, hybrid retrieval, and the grounded chat endpoint. Then Stage 5 (now unblocked): Contact lead capture to the admin CRM intake and the Solution Finder. Re-export a crisp vector logo and pull the Covyvo/GLEEN mockups when the Figma quota resets.",
  "blockedOn": null,
  "stages": [
    {
      "id": "stage-0",
      "title": "Monorepo scaffold",
      "status": "done",
      "tasks": [
        { "id": "repo-init", "status": "done", "notes": "Standalone git repo initialised at nexoristech with main branch." },
        { "id": "control-system", "status": "done", "notes": "/.nexoris build-state.json, BUILD_PROGRESS.md, DECISIONS.md created." },
        { "id": "root-config", "status": "done", "notes": "package.json, pnpm-workspace.yaml, turbo.json, .gitignore, .gitattributes, base tsconfig, .env.example, .npmrc, .nvmrc, root eslint/prettier." },
        { "id": "packages-scaffold", "status": "done", "notes": "packages/config (tokens + Tailwind brand preset), ui, seo, kb, brand scaffolded; type-check, lint, test, build green." },
        { "id": "apps-scaffold", "status": "done", "notes": "apps/web, cms, oge, admin workspace packages created; frameworks installed in their stages." },
        { "id": "docker-compose", "status": "done", "notes": "infra/docker-compose.yml with pgvector pg16 + Meilisearch v1.11 pinned; DB init script for three databases." },
        { "id": "ci-skeleton", "status": "done", "notes": ".github/workflows/ci.yml runs lint, type-check, test, build, check:seo, check:a11y; the two gates are failing stubs." },
        { "id": "docs-and-copy", "status": "done", "notes": "docs/PRD.md and content-source 00-04 placed verbatim as read-only source of truth." }
      ]
    },
    {
      "id": "stage-1",
      "title": "packages/seo and the check:seo gate",
      "status": "done",
      "tasks": [
        { "id": "schema-builders", "status": "done", "notes": "JSON-LD @graph builders for site nodes, WebPage subtypes, BreadcrumbList, ImageObject, Service, FAQPage, Article/BlogPosting, ProfilePage, JobPosting, case study; NG locale; prune omission rule; buildPageGraph assembler; unit tested." },
        { "id": "build-metadata", "status": "done", "notes": "buildMetadata enforces title <=60 and description 155-160 (160 hard max); trailing-slash-consistent canonical/OG with bare home root; validateMeta for the gate; unit tested." },
        { "id": "og-card", "status": "done", "notes": "Framework-agnostic OG card model and brand spec in packages/seo. PNG rendering (1200x630) wired in apps/web opengraph-image route once brand fonts/logo land (Stage 2/4); recorded in DECISIONS D-007." },
        { "id": "sitemaps-robots-llms", "status": "done", "notes": "Split sitemaps + index, robots.txt, llms.txt built from the real catalogue; unit tested." },
        { "id": "sitelinks-eligibility", "status": "done", "notes": "WebSite SearchAction and BreadcrumbList implemented; flat-architecture link rules to be enforced when pages exist (Stage 4) and verified by check:seo." },
        { "id": "check-seo-gate", "status": "done", "notes": "Real rule engine over the build manifest asserting every PRD 9.11 rule, with a CLI anchored to the workspace root; passes vacuously with no pages and turns red on any real violation; 60 unit tests." }
      ]
    },
    {
      "id": "stage-2",
      "title": "packages/ui design system + check:a11y gate",
      "status": "done",
      "tasks": [
        { "id": "design-tokens", "status": "done", "notes": "Tailwind brand preset consumed by packages/ui; globals.css applies the focus ring, reduced-motion rules, and skip link." },
        { "id": "typography-contexts", "status": "done", "notes": "fonts.css @font-face for the three contexts; self-hosted variable woff2 (OFL) stored in packages/brand/assets/fonts." },
        { "id": "header-flyouts", "status": "done", "notes": "Flyout disclosure (aria-expanded/controls, Enter/Down open, Escape close with focus return, click-outside) plus MegaMenu and polymorphic MegaMenuItem for Services/Industries/Company. The Header composition with logo and scroll state is wired in apps/web (Stage 4)." },
        { "id": "mobile-drawer-toc", "status": "done", "notes": "MobileNav full-screen dialog with focus trap, Escape, body scroll lock, sticky footer; AccordionItem; FloatingTableOfContents with section highlight, focus trap, and reduced-motion instant jump." },
        { "id": "responsive-tables", "status": "done", "notes": "ResponsiveTable with stacked-card and frozen-first-column scroll patterns, caption with announced row count, scoped headers, JetBrains Mono right-aligned numerics." },
        { "id": "four-states", "status": "done", "notes": "DataState (loading/empty/error/populated) with Skeleton, EmptyState using approved copy, ErrorState with retry; never colour-only." },
        { "id": "check-a11y-gate", "status": "done", "notes": "Real axe-core component gate failing on serious/critical; passes at the root. Route-level Playwright axe (including contrast) is added in Stage 4 (decision recorded)." }
      ]
    },
    {
      "id": "stage-3",
      "title": "Hardcoded content modules from approved copy",
      "status": "done",
      "tasks": [
        { "id": "content-system", "status": "done", "notes": "Typed content-module schema (discriminated section model incl. form and dynamic kinds) in apps/web/src/content." },
        { "id": "content-fidelity-test", "status": "done", "notes": "Whitespace-normalised byte-diff of every rendered string (metas, H1s, sublines, CTAs, headings, card/step/FAQ copy) against content-source. Runs in CI." },
        { "id": "core-pages", "status": "done", "notes": "Home, About, How We Work, Case Studies hub, Contact, all verbatim and passing fidelity." },
        { "id": "service-pages", "status": "done", "notes": "All 11 service pages verbatim and passing fidelity." },
        { "id": "industry-pages", "status": "done", "notes": "All 20 industry pages transcribed verbatim and passing fidelity." }
      ]
    },
    {
      "id": "stage-4",
      "title": "33 hardcoded marketing pages",
      "status": "in_progress",
      "tasks": [
        { "id": "core-pages", "status": "not_started", "notes": "Home, About, How We Work, Contact, Case Studies hub." },
        { "id": "service-pages", "status": "not_started", "notes": "All 11 service pages in the prescribed section order." },
        { "id": "industry-pages", "status": "not_started", "notes": "All 20 industry pages in the prescribed section order." },
        { "id": "content-api-proof", "status": "not_started", "notes": "Cached content API supplying case study cards and testimonials; absent proof renders nothing." },
        { "id": "schema-og-cwv", "status": "not_started", "notes": "All schema and OG live; Core Web Vitals green." }
      ]
    },
    {
      "id": "stage-5",
      "title": "Contact lead capture + Solution Finder",
      "status": "not_started",
      "tasks": [
        { "id": "contact-form", "status": "done", "notes": "DONE and verified live end to end: apps/web ContactForm client component renders the approved fields from the content module, composes a lead, and posts to the Next /api/contact/ proxy -> Oge /leads, which scores it (CRM Worker, rules fallback) and stores it in the nexoris_admin lead table, returning a warm acknowledgement (scoring kept internal). Shows the approved after-submit message on success and a reach-us-directly fallback if the gateway is down. Slotted into the contact page form section. Lead intake backend (LeadsService + POST /leads + nexoris_admin lead table, DECISIONS D-016 boundary) also done." },
        { "id": "smart-form-assistant", "status": "not_started", "notes": "Debounced one-sentence hint; deterministic fallback." },
        { "id": "instant-ack", "status": "not_started", "notes": "Personalised acknowledgement on submit; warm template fallback." },
        { "id": "solution-finder", "status": "in_progress", "notes": "Home embedded Solution Finder DONE and verified live end to end: apps/web SolutionFinder client component renders the five questions from @nexoris/recommend, posts to the Next /api/finder/ proxy -> Oge /finder, shows the deterministic services + industry page links and the AI rationale, and falls back to the local deterministic match if the gateway is unreachable. Slotted into the home solution-finder section. Remaining entry points: the Services flyout featured panel and the ghost CTA on every service and industry page." }
      ]
    },
    {
      "id": "stage-6",
      "title": "apps/oge AI gateway",
      "status": "not_started",
      "tasks": [
        { "id": "model-registry", "status": "done", "notes": "apps/oge/src/config/models.ts pins the new five-group provider stack (DECISIONS D-012): WEBSITE_BOT, CRM_WORKER, CMS_AI, SERVICE_RECOMMENDER generation groups each with primary + two backups, plus EMBEDDING_MODELS (Mistral Embed primary, Gemini fallback, 1024-dim). SambaNova fully removed. Pure helpers (apiKeyEnvVar with Project A/B routing, resolveModelId env override, chainFor, apiKeyFor) and 12 tests. Identifiers are provisional defaults, env-overridable, flagged to verify against provider docs. apps/oge wired as an ESM package (type-check, lint, test, build green)." },
        { "id": "fallback-orchestrator", "status": "done", "notes": "src/providers/errors.ts (RateLimit/ProviderUnavailable/SchemaValidation retriable taxonomy + fatal ChainExhaustedError) and src/orchestration/run-chain.ts: provider-agnostic runChain walks a group primary-first, skips slots with no key, advances on retriable errors, returns the slot used, surfaces non-retriable errors, throws ChainExhaustedError when spent. 7 tests, network work injected." },
        { "id": "db-schema", "status": "done", "notes": "nexoris_oge embedding store provisioned and verified live (DECISIONS D-013): apps/oge/migrations/0001_init.sql + a forward-only tsx runner (scripts/migrate.ts, pg, loads the gitignored .env via process.loadEnvFile, tracked in _oge_migration, idempotent). Tables: kb_chunk (embedding vector(1024), HNSW cosine index, source-url + version indexes), exact_match_cache, semantic_cache (prompt_embedding vector(1024), HNSW), gap_report. Applied to the live DB; re-run is a no-op." },
        { "id": "kb-ingestion", "status": "in_progress", "notes": "Hardcoded half DONE and verified live: apps/web kb:export emits artifacts/knowledge-base.json (66 chunks from the 36 pages, content-hash kbVersion); apps/oge src/providers/embeddings.ts walks the embedding chain (Mistral Embed primary, Gemini 1024-dim fallback) over REST; scripts/ingest.ts embeds once per version (skips already-embedded chunks on re-run), upserts into kb_chunk with prune-by-version, and indexes all chunks into Meilisearch (best-effort). Real Mistral Embed pass stored 66/66 vectors (all 1024-dim, 36 pages); Meilisearch kb_chunk index holds 66 docs. Remaining: CMS publish re-ingestion webhooks (Stage 7)." },
        { "id": "hybrid-retrieval", "status": "done", "notes": "src/search/meilisearch.ts (REST client with task polling), src/retrieval/fuse.ts (reciprocal rank fusion, 4 tests), and src/retrieval/retrieve.ts (embeds the query, runs pgvector cosine + Meilisearch keyword in parallel, fuses, dedupes; degrades to vector-only if Meilisearch is down). Verified live via scripts/retrieve.ts: 'chatbot to reduce missed appointments at a clinic' surfaces both AI Chatbots & Virtual Assistants and Healthcare, matching the PRD 10.7 expectation. Meilisearch v1.47.0 at 194.147.95.7:7700 confirmed (DECISIONS open question resolved)." },
        { "id": "caches-quota", "status": "done", "notes": "src/cache: normalise (query hash + version, tested), ExactMatchCache (DB, hit-count bump; live-smoked), SemanticCache (pgvector cosine; threshold recalibrated to 0.88 for Mistral Embed per DECISIONS D-014, env-overridable). src/quota: TokenBucket (injectable clock, tested) and QuotaGovernor (per-session + per-IP fixed windows, 2000-char input cap, per-provider buckets, batch-defer reserve; tested). 38 tests total." },
        { "id": "website-bot-chain", "status": "done", "notes": "src/providers/generation.ts: Gemini generateContent + Groq/Mistral OpenAI-compatible chat completions, generateGrounded walks WEBSITE_BOT_MODELS via runChain (empty completion advances). Wired in OgeService behind exact then semantic cache, with the quota governor in front. Live: re-ask hit exact-cache with zero model calls." },
        { "id": "grounded-chat", "status": "done", "notes": "NestJS runtime (main.ts, app.module, OgeService, ChatController SSE, HealthController) compiled to dist and run on Node (tsconfig overridden to NodeNext + decorators for Nest DI). Verbatim Oge system prompt in src/runtime/prompt.ts (tested). Live-verified end to end against the VPS DB + Mistral + Gemini + Meilisearch: /health ok, grounded answers stream over SSE with sources; PRD 10.7 pricing test returns no invented figure and explains the written scope; off-topic 'write me a poem' gets a one-sentence decline. Test cache rows cleared afterwards so production caches start clean. Web front end DONE: apps/web OgeWidget (floating button with one first-visit pulse, dialog panel, SSE streaming, sources and handoff rendering) mounted in the layout so Oge floats on all pages, talking to the gateway via the /api/chat/ SSE proxy (graceful offline notice if the gateway is down). Verified live through the proxy." },
        { "id": "lead-capture-finder-rationale", "status": "in_progress", "notes": "Solution Finder rationale DONE and verified live: new @nexoris/recommend package (registry, PRD 8 matrix, owner-approved headache mapping, deterministic matcher, 7 tests) + Oge POST /finder (FinderService/Controller) returns the deterministic recommendation plus a short rationale via SERVICE_RECOMMENDER_MODELS under strict JSON, with a deterministic template fallback. Live: healthcare+missed-messages returned AI Chatbots + healthcare page with an AI rationale (served by the Qwen Groq backup; Gemini primary was rate-limited, proving failover); parseRationale strips reasoning-model <think> blocks; invalid input rejected with 400. Remaining: Oge conversational lead capture to CRM intake (pairs with Stage 5 + admin Stage 9)." },
        { "id": "extractive-fallback", "status": "done", "notes": "OgeService returns the verbatim PRD 10.2 preamble plus the top retrieved chunk as a quotation and a handoff event (WhatsApp, contact page, business email) when the generation chain is exhausted; empty retrieval declines plainly with the same handoff. ChatController converts any error to a plain notice, never a stack trace or status code." }
      ]
    },
    {
      "id": "stage-7",
      "title": "Strapi CMS",
      "status": "in_progress",
      "tasks": [
        { "id": "scaffold", "status": "done", "notes": "apps/cms scaffolded as Strapi v5 TS (@nexoris/cms), wired to nexoris_cms via env (pg, pool min 0 for the remote VPS that drops idle conns). Verified live: admin builds, Strapi starts, migrates, serves /admin. .env gitignored." },
        { "id": "content-types", "status": "done", "notes": "DONE and verified live (13 tables created in nexoris_cms). Collection types: author + fact-checker, category, insight (EEAT), testimonial, case-study (with metrics), job (careers, JobPosting fields). Single types: privacy-policy, terms-of-service, cookie-policy (section + plain-summary pairs). Shared components: seo (meta limits + index toggle), faq-item, metric, legal-section. type-check + admin build green. pseo-page is deferred to Stage 11 (the programmatic layer it belongs to)." },
        { "id": "content-api", "status": "done", "notes": "apps/web/src/lib/cms.ts: typed, ISR-cached content-API client with graceful empty fallback (CMS down or no content renders nothing). Wired into the home proof band (featured case studies + metrics), testimonials, and latest-Insights slots, and the Insights hub (/insights) and article (/insights/[slug], BlogPosting + FAQPage + breadcrumb JSON-LD, react-markdown body, EEAT author/fact-checker, TL;DR, FAQ). Public read permissions bootstrap verified (endpoints 200). Build green (42 routes). Populated render will light up with real content; empty path verified at build and via graceful fallback." },
        { "id": "seed-preview-webhooks", "status": "in_progress", "notes": "Publish webhooks DONE: a Strapi document middleware on publish/unpublish/delete revalidates apps/web ISR routes (/api/revalidate/, shared secret, 200/401 verified) and re-ingests the page into Oge (/reingest upsert/delete, verified live earlier). React types pinned to 19 workspace-wide (pnpm override) to resolve the Strapi React 18 conflict. Remaining: starter/legal seed and preview routes. Note: the live publish-trigger e2e (admin creates+publishes an article) is not yet exercised end to end, but each link is verified and the middleware registers cleanly." },
        { "id": "url-field-index-toggle", "status": "not_started", "notes": "Read-only full public URL field; plain-language index/de-index toggle." },
        { "id": "redirect-manager", "status": "not_started", "notes": "301/302 redirect manager; collision/chain/loop checks; auto-301 on slug change." },
        { "id": "date-tokens", "status": "not_started", "notes": "[year], [month], [month-year] resolved at render in Africa/Lagos." },
        { "id": "seed-preview-webhooks", "status": "not_started", "notes": "Legal/starter seed; preview routes; revalidation + Oge re-ingestion webhooks." }
      ]
    },
    {
      "id": "stage-8",
      "title": "Insights, authors, careers, legal, AI search",
      "status": "in_progress",
      "tasks": [
        { "id": "insights-hub", "status": "done", "notes": "/insights card grid, latest-first, excerpt + date, empty state; homepage latest three wired. Build green." },
        { "id": "article-page", "status": "in_progress", "notes": "/insights/[slug] done: BlogPosting + FAQPage + breadcrumb JSON-LD, react-markdown body, author + fact-checker, TL;DR, FAQ, author bio. Remaining EEAT extras: short-title TOC, recommended reads, internal-link suggestions, AI bio block." },
        { "id": "author-pages", "status": "done", "notes": "/authors/[slug] done: ProfilePage + Person schema, role/bio/LinkedIn, authored-articles list. Build green." },
        { "id": "careers", "status": "done", "notes": "/careers hub + /careers/[slug] done: JobPosting schema, employment-type labels, markdown description, apply CTA (url/email/contact fallback), empty state. Cover images pending media." },
        { "id": "legal-pages", "status": "done", "notes": "/privacy-policy, /terms-of-service, /cookie-policy render the Strapi single types as section + plain-summary pairs (shared LegalPageView), with an interim note until published. Build green." },
        { "id": "ai-search-mobile-toc", "status": "not_started", "notes": "AI search; mobile floating TOC; CMS AI helpers wired." }
      ]
    },
    {
      "id": "stage-9",
      "title": "apps/admin shell + CRM module",
      "status": "in_progress",
      "tasks": [
        { "id": "scaffold", "status": "done", "notes": "apps/admin scaffolded as Next.js 15 App Router (@nexoris/admin), consuming @nexoris/ui + the brand preset, connected to nexoris_admin via src/lib/db (pool min 0). CRM schema migration applied and verified: staff, audit_log, lead_activity, and lead extended with assigned_to/sla_due_at/lost_reason/nurture_date. Dashboard shell (left module nav) + CRM lead list (/crm, reads real leads ordered by score, band badges, empty state) build and run live (/ redirects to /crm, DB connected). type-check/lint/build green." },
        { "id": "dashboard-shell", "status": "done", "notes": "Sign-in DONE and verified live: stateless HMAC-signed session cookie (src/lib/session), getCurrentStaff re-checks active status in the DB each request, requireStaff/requireAdmin gate the (dashboard) route group, login page + signIn/signOut server actions (bcrypt), seed-admin script (db:seed:admin, env-driven, idempotent). /crm and /crm/[id] redirect to /login when unauthenticated (307); /login renders. Roles: admin/salesperson/viewer enforced (viewer read-only). Audit log written on stage changes." },
        { "id": "crm-core", "status": "in_progress", "notes": "Lead list + detail + stage transitions DONE. People management DONE (/people, admin-only): staff list with open-lead counts, add-staff form (bcrypt, role, industries, capacity), deactivate (returns open leads to the queue, preserves history). Assignment DONE: pure chooseAssignee (industry affinity + capacity + load balance, 6 tests), admin assign/unassign + auto-assign on the lead detail, all audited. Remaining: SLA timer + alerts, targets/dashboards/SLA board, true on-intake auto-assignment (currently admin-driven; oge intake leaves leads unassigned)." },
        { "id": "targets-dashboards", "status": "in_progress", "notes": "Dashboard (/dashboard) DONE: first-response SLA board (one-business-day deadline via tested businessDayDeadline helper, most-urgent-first, breach flag), pipeline by stage, source performance (total + won), win rate, and headline stats. Role-scoped (salesperson sees own, admin/viewer see all). Remaining: targets table + setting UI + targets-vs-actuals, revenue/deal value, avg first-response-time metric, activity quick-log, scheduled weekly/monthly digest views + export." },
        { "id": "crm-ai", "status": "in_progress", "notes": "Scoring (justification) done in oge. Auto-response drafting DONE: oge POST /crm/draft (CRM Worker, internal-secret auth) drafts a warm grounded reply (drafts-only, no invented price/date), verified live; admin lead detail has DraftReply (editable, review-before-send) via a server action. Service recommendations DONE: deterministic @nexoris/recommend match from the lead's finder answers shown with page links (PRD 3.3). Em-dash house rule now enforced in code on every generated answer (stripEmDash in generateGrounded, tested). Remaining smart features (summaries, technical-fit, talking points, dedup, digests) and PDF engine." },
        { "id": "smart-features", "status": "not_started", "notes": "Summaries, technical-fit, talking points, dedup, urgency/sentiment, next-best-action, digests." },
        { "id": "pdf-engine", "status": "not_started", "notes": "Branded document engine: proposals, SOWs, SLAs, contracts, invoices; server-side selectable PDFs." }
      ]
    },
    {
      "id": "stage-10",
      "title": "Hardening + launch runbook",
      "status": "in_progress",
      "tasks": [
        { "id": "cwv-lighthouse-gates", "status": "not_started", "notes": "Core Web Vitals + Lighthouse CI budgets enforced." },
        { "id": "security-headers", "status": "done", "notes": "Security headers on apps/web and apps/admin via next.config headers(): CSP (same-origin, frame-ancestors none), HSTS, X-Content-Type-Options, X-Frame-Options DENY, Referrer-Policy, Permissions-Policy. Verified live on apps/web. Rate limiting DONE: in-memory per-IP limiter on the public endpoints (/api/chat 20/min graceful SSE notice, /api/contact 5/min -> 429), verified live; the oge quota governor remains the per-provider/session layer." },
        { "id": "cookie-consent", "status": "done", "notes": "CookieConsent banner (granular necessary/analytics/personalisation toggles, Accept all / Reject non-essential / Save choices), choice stored in a first-party nx-consent cookie so the server can gate analytics and personalisation, linked to /cookie-policy, shown once. Mounted in the web layout." },
        { "id": "error-pages", "status": "done", "notes": "404 and error pages in the house voice (delivered in Stage 4)." },
        { "id": "launch-runbook", "status": "done", "notes": "docs/RUNBOOK.md: env, build, DB migrations + admin seed, service start sequence, KB ingest, webhooks, DNS/Search Console/sitemaps, provider-fallback smoke tests, post-launch. Remaining stage-10: Lighthouse/CWV CI budgets, route-level Playwright axe, share-debugger checks." }
      ]
    },
    {
      "id": "stage-11",
      "title": "Programmatic SEO layer",
      "status": "not_started",
      "tasks": [
        { "id": "template-route-guard", "status": "not_started", "notes": "Template route with quality-gate publish guard and data-readiness gate; unpublished by default." },
        { "id": "proof-ladder", "status": "not_started", "notes": "Substitution ladder so no page renders empty and none fabricates." },
        { "id": "first-wave", "status": "not_started", "notes": "Roughly 10 to 20 fully data-complete permutation and cost pages from in-house products and strongest local data." },
        { "id": "readiness-dashboard", "status": "not_started", "notes": "CMS readiness dashboard; article-derived pipeline; quarterly AI-visibility tracking; first annual report." }
      ]
    }
  ],
  "openQuestions": [
    "Confirm the semantic-cache threshold change (DECISIONS D-014): PRD 10.2 specifies 0.92, but measured Mistral Embed paraphrases score 0.85-0.89, so the default was lowered to 0.88 (env-overridable). Confirm 0.88 or supply a preferred value.",
    "Confirm the lead-scoring rubric and ideal customer profile (DECISIONS D-015): the ICP is grounded in the approved positioning and the home trust strip, band thresholds are Hot 70+/Warm 45-69/Cold <45, and the rules-baseline weights are a tunable default. Confirm or adjust the ICP wording and weights.",
    "Confirm the VPS media public base URL: only the disk path /www/docker/nexoristech-media was supplied; .env carries a provisional https://nexoristech.com/media.",
    "Regenerate strong production values for the placeholder shared/Strapi/admin secrets and the Meilisearch master key in .env before deploy.",
    "Nexoris Technologies logo source files: re-export a crisp vector and pull the Covyvo/GLEEN mockups when the Figma quota resets."
  ]
}
