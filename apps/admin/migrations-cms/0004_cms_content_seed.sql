-- Intentionally empty.
--
-- This generated roughly 2,700 rows of content across every kind — programmatic pages, insights,
-- testimonials, case studies, job posts — so that listings, counts and charts had something to draw.
-- None of it was real, and a platform preparing for real content must not start with it.
--
-- Kept as a no-op so the filename stays recorded in ledgers that already applied it. See 0002.
SELECT 1;
