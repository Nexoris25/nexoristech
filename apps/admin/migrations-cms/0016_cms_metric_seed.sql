-- Intentionally empty.
--
-- This filled cms_metric_daily with 760 days of clicks, impressions and average positions generated
-- with random(). Traffic figures that nobody measured are the most damaging kind of invented data:
-- they are indistinguishable from Search Console's own numbers once stored, and decisions get made on
-- them.
--
-- The table fills from the real Search Console and GA4 readers. Until those are connected it stays
-- empty, and every chart that reads it says so.
SELECT 1;
