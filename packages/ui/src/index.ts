/**
 * @nexoris/ui
 *
 * The Nexoris Technologies design system. Built in Stage 2, before any page, so every page
 * is born accessible and on-brand. Populated with the three font contexts, the
 * keyboard-correct header flyouts, the mobile navigation drawer, the mobile floating table of
 * contents, the responsive table patterns, and the loading, empty, error, and populated
 * states for every data surface.
 */

export const UI_PACKAGE = "@nexoris/ui" as const;
