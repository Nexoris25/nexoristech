/**
 * @nexoris/seo
 *
 * The SEO engine for the Nexoris Technologies platform. Built before any page (delivery plan
 * Stage 1) so every page is born fast, accessible, and SEO valid. This entry point is
 * populated in Stage 1 with the JSON-LD @graph builders, the buildMetadata helper, the
 * branded Open Graph card generator, the split sitemaps, robots.txt, and llms.txt.
 */

export const SEO_PACKAGE = "@nexoris/seo" as const;
