# Install only from the official npm registry.
registry=https://registry.npmjs.org/

# Strict, reproducible installs.
auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false
save-exact=true
